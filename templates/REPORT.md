# REPORT — <piece name>

> English, ≤ 1 page. No code dumps. The same shape is used by the Sonnet tester (Step 7).

## What changed
- `<file>:<line>` — <one line>
- `<file>:<line>` — <one line>

## What was run and the result
| Command | Result |
|---|---|
| `<command>` | <pass / fail — N tests, names of failing tests> |

For a failure: the 5–10 relevant log lines, and whether it also reproduces on the base
branch (pre-existing) or is caused by the change.

## Unverified
- <what was not checked and why>

## Open questions
- <question> (or "none")
